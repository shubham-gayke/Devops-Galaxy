// App.jsx — retained for backwards-compatibility only.
// The application now uses React Router (src/app/router.jsx).
// The root layout is src/components/Layout/Layout.jsx.
// This file is no longer used but kept to avoid breaking any
// potential external references during the transition.
export { default } from './components/Layout/Layout'
